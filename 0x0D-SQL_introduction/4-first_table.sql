-- Task creates a table
-- Script creates a table called `first_table` in current directory
-- with columns: `id` - INT, `name` - VARCHAR(256)
CREATE TABLE IF NOT EXISTS first_table (
       id INT,
       name VARCHAR(256)
);
